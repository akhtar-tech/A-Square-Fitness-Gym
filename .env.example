# API Configuration
VITE_API_URL=http://localhost:3000/api/v1

# Note: The backend now enforces CORS. Make sure your frontend URL
# is added to the backend's ALLOWED_ORIGINS environment variable.
# Example backend .env:
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
